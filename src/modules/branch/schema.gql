type Query {
    branches(
        pagination: Pagination
        search: String
        sort: Sort
    ): [Branch!]!
    branch(branchId: ID!): Branch
}

type Mutation {
    register(
        branchId: Int!
        staffName: String!
        password: String!
        birthDate: String!
        gender: String!
    ): MutationResponse!



    addBranch(
        staffName: String!
        password: String!
    ): MutationResponse!


    changeBranch(
        userId: ID!
        username: String
        contact: String
    ): MutationResponse!

    deleteBranch(userId: ID!): MutationResponse!
}

type Branch {
    branchId: ID!
    branchName: String!
    branchAddress: String!
    time: String!
}